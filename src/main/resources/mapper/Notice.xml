<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pigs.holiday.mapper.admin.NoticeMapper">

    <!--Detail-->
    <select id="detail" parameterType="hashMap" resultType="com.pigs.holiday.dto.admin.NoticeDto$DetailResDto">
        SELECT notice.id,
               notice.deleted,
               notice.created_at as createdAt,
               notice.modified_at as modifiedAt,
               notice.title,
               notice.content
        FROM notice
        WHERE notice.id = #{id}
    </select>

    <!--List-->
    <select id="list" parameterType="hashMap" resultType="com.pigs.holiday.dto.admin.NoticeDto$ListResDto">
        SELECT notice.id,
               notice.title,
               notice.deleted,
               notice.create_user_id as createUserId,
               notice.created_at as createdAt,
               notice.modified_at as modifiedAt
        FROM notice
        WHERE notice.id is not NULL
    </select>

</mapper>